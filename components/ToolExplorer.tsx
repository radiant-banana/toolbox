"use client";import{useMemo,useState}from"react";import Link from"next/link";import type{Tool}from"@/content/tools";import s from"./ToolExplorer.module.css";type Filter="all"|Tool["category"];
export function ToolExplorer({tools}:{tools:readonly Tool[]}){const[f,setF]=useState<Filter>("all");const shown=useMemo(()=>f==="all"?tools:tools.filter(t=>t.category===f),[f,tools]);return <><div className={s.filters}>{(["all","text","data","generator"]as Filter[]).map(x=><button key={x} className={f===x?s.active:""} aria-pressed={f===x} onClick={()=>setF(x)}>{x==="generator"?"Generators":x[0].toUpperCase()+x.slice(1)}</button>)}</div><div className={s.grid}>{shown.map(t=>{const body=<><div className={s.top}><span className={s.icon}>{t.category[0].toUpperCase()}</span><span className={s.status}>{t.status==="ready"?"Ready":"Coming soon"}</span></div><div className={s.body}><h3>{t.title}</h3><p>{t.description}</p><b>{t.status==="ready"?"Open tool ->":"On the workbench"}</b></div></>;const cn=`${s.card} ${t.featured?s.featured:""} ${t.status==="soon"?s.disabled:""}`;return t.status==="ready"?<Link className={cn} href={`/${t.slug}/`} key={t.slug}>{body}</Link>:<article className={cn} key={t.slug}>{body}</article>})}</div></>}
